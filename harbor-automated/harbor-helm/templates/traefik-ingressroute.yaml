{{- if and .Values.expose.traefik.enabled (eq .Values.expose.type "traefik") }}
---
apiVersion: {{ .Values.expose.traefik.apiVersion }}
kind: IngressRoute
metadata:
  name: {{ include "harbor.fullname" . }}-https
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "harbor.labels" . | nindent 4 }}
spec:
  entryPoints:
    - websecure
  routes:
    # API routes (highest priority - most specific)
    - match: Host(`{{ .Values.expose.traefik.host }}`) && PathPrefix(`/api/`)
      kind: Rule
      priority: 30
      services:
        - name: {{ include "harbor.fullname" . }}-core
          port: 80
      {{- if .Values.expose.traefik.middlewares }}
      middlewares:
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
    
    # Chart repository API
    - match: Host(`{{ .Values.expose.traefik.host }}`) && PathPrefix(`/chartrepo/`)
      kind: Rule
      priority: 25
      services:
        - name: {{ include "harbor.fullname" . }}-core
          port: 80
      {{- if .Values.expose.traefik.middlewares }}
      middlewares:
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
    
    # Registry API (v2)
    - match: Host(`{{ .Values.expose.traefik.host }}`) && PathPrefix(`/v2/`)
      kind: Rule
      priority: 20
      services:
        - name: {{ include "harbor.fullname" . }}-registry
          port: 5000
      {{- if .Values.expose.traefik.middlewares }}
      middlewares:
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
    
    # Core API endpoints (login, CSRF, etc.)
    - match: Host(`{{ .Values.expose.traefik.host }}`) && PathPrefix(`/c/`)
      kind: Rule
      priority: 18
      services:
        - name: {{ include "harbor.fullname" . }}-core
          port: 80
      {{- if .Values.expose.traefik.middlewares }}
      middlewares:
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
    
    # Service endpoints (for replication and webhooks)
    - match: Host(`{{ .Values.expose.traefik.host }}`) && PathPrefix(`/service/`)
      kind: Rule
      priority: 15
      services:
        - name: {{ include "harbor.fullname" . }}-core
          port: 80
      {{- if .Values.expose.traefik.middlewares }}
      middlewares:
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
    
    # Core Harbor UI (lowest priority - catch-all)
    - match: Host(`{{ .Values.expose.traefik.host }}`)
      kind: Rule
      priority: 10
      services:
        - name: {{ include "harbor.fullname" . }}-portal
          port: 80
      {{- if or .Values.expose.traefik.middlewares .Values.expose.traefik.ipWhitelist.enabled }}
      middlewares:
        {{- if .Values.expose.traefik.ipWhitelist.enabled }}
        - name: {{ include "harbor.fullname" . }}-ip-whitelist
        {{- end }}
        {{- range .Values.expose.traefik.middlewares }}
        - name: {{ . }}
        {{- end }}
      {{- end }}

  {{- if .Values.expose.traefik.tls.enabled }}
  tls:
    {{- if .Values.expose.traefik.tls.certResolver }}
    certResolver: {{ .Values.expose.traefik.tls.certResolver }}
    {{- end }}
    {{- if .Values.expose.traefik.tls.secretName }}
    secretName: {{ .Values.expose.traefik.tls.secretName }}
    {{- end }}
  {{- end }}

{{- if .Values.expose.traefik.httpsRedirect.enabled }}
---
apiVersion: {{ .Values.expose.traefik.apiVersion }}
kind: IngressRoute
metadata:
  name: {{ include "harbor.fullname" . }}-http
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "harbor.labels" . | nindent 4 }}
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ .Values.expose.traefik.host }}`)
      kind: Rule
      services:
        - name: {{ include "harbor.fullname" . }}-portal
          port: 80
      middlewares:
        - name: {{ include "harbor.fullname" . }}-https-redirect
        {{- if .Values.expose.traefik.ipWhitelist.enabled }}
        - name: {{ include "harbor.fullname" . }}-ip-whitelist
        {{- end }}
{{- end }}
{{- end }}
